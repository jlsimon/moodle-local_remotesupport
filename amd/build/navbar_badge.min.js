/**
 * Keeps the navbar pending-requests badge (navbar_requests.mustache, see
 * lib.php's local_remotesupport_render_navbar_output()) up to date without a
 * full page reload. Loaded on every Moodle page for any user able to provide
 * assistance in at least one course — deliberately the lightest poll in the
 * plugin (15 s, no per-row data, a single count), since it runs sitewide
 * rather than only on the plugin's own pages.
 *
 * @module     local_remotesupport/navbar_badge
 * @copyright  2026 Juan Luis Simón
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_remotesupport/navbar_badge",["core/templates","local_remotesupport/session_requests"],(function(Templates,SessionRequests){return{init:function(){var container=document.getElementById("local-remotesupport-navbar-requests");if(container){var link=container.querySelector("a"),url=link?link.getAttribute("href"):"",pollHandle=null,startPolling=function(){null===pollHandle&&(pollHandle=window.setInterval(refresh,15e3))},refresh=function(){return SessionRequests.getPendingCount().then((function(result){return Templates.renderForPromise("local_remotesupport/navbar_requests",{count:result.count,haspending:result.count>0,supportenabled:result.supportenabled,url:url})})).then((function(result){return Templates.replaceNodeContents(container,result.html,result.js),null})).catch((function(){}))};document.addEventListener("visibilitychange",(function(){document.hidden?null!==pollHandle&&(window.clearInterval(pollHandle),pollHandle=null):(refresh(),startPolling())})),startPolling()}}}}));

//# sourceMappingURL=navbar_badge.min.js.map