{
  "name": "local_remotesupport-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Jest unit tests for local_remotesupport's AMD modules. Not shipped with the plugin.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
